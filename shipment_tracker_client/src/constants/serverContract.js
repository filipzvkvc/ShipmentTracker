const SERVER_ATTRIBUTES = {
  S_USER : {
    USER_ID : "user_id",
    FULL_NAME : "full_name",
    EMAIL : "email",
    PASSWORD : "password",
    USER_TYPE : "user_type"
  }
}

const SERVER_MODELS_NAMES = {
  S_USER : "users"
}

export { SERVER_ATTRIBUTES as S_ATTR, SERVER_MODELS_NAMES as S_MODELS}
export const { S_USER } = SERVER_ATTRIBUTES;